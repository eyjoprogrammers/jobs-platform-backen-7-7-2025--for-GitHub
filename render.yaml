services:
  - type: web
    name: jobs-api
    env: php
    buildCommand: "composer install && php artisan config:clear && php artisan route:cache"
    startCommand: "php artisan serve --host=0.0.0.0 --port=10000"
    plan: free
